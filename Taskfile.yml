# https://taskfile.dev

version: '3'

includes:
  pypi:
    taskfile: ./tasks/pypi.tasks.yml
    dir: .

vars:
  GREETING: Hello, World!

tasks:

  default:
    cmds:
      - echo "{{.GREETING}}"
      - task -l
    silent: true

  test:
    desc: _
    cmds:
      - task run-all:full -d tests
